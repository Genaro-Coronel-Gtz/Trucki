extends Node

@onready var ui_scene_path = "res://ui.tscn"

#func _ready():
	## Cambiar a la escena del UI
	#var ui_scene = load(ui_scene_path)
	#if ui_scene_path:
		#get_tree().change_scene_to_packed(ui_scene)
	#else:
		#print("Error: No se pudo cargar la escena del UI.")
